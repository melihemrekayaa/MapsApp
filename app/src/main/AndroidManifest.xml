<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mapsapp">
    <!-- 📌 Gerekli İzinler -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" /> <!-- 📌 Android 13 (API 33+) İçin Özel İzinler -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MapsApp">
        <activity
            android:name=".webrtc.ui.CallingActivity"
            android:exported="false" />
        <!-- 📌 Başlangıç (Launcher) Aktivitesi -->
        <activity
            android:name=".MapsMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 📌 WebRTC Arama Aktivitesi -->
        <activity
            android:name=".webrtc.ui.WebRTCMainActivity"
            android:exported="true" />
        <activity
            android:name=".webrtc.ui.CallActivity"
            android:exported="true" />
        <activity
            android:name=".webrtc.ui.CloseActivity"
            android:exported="true" /> <!-- 📌 WebRTC Servisi -->
        <service
            android:name=".webrtc.service.MainService"
            android:exported="false"
            android:foregroundServiceType="camera|microphone"
            android:permission="android.permission.FOREGROUND_SERVICE" /> <!-- 📌 WebRTC Broadcast Receiver -->
        <receiver
            android:name=".webrtc.service.MainServiceReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="REJECT_CALL" />
                <action android:name="ACCEPT_CALL" />
            </intent-filter>
        </receiver>
    </application>

</manifest>