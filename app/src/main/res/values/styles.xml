<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Aktif metin için stil -->
    <style name="ActiveTextStyle" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#000000</item> <!-- Siyah renk -->
    </style>

    <!-- İnaktif metin için stil -->
    <style name="InactiveTextStyle" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">#666666</item> <!-- Gri renk -->
    </style>

    <style name="CustomDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowMinWidthMajor">90%</item> <!-- Genişliği ekranın %90'ı yapar -->
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
